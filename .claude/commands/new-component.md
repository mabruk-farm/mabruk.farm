---
description: Create a new Svelte 5 component with TypeScript and Tailwind
allowed-tools: Read, Write, Edit, Glob, Grep
---

Create a new Svelte 5 component for the Mabruk Farm website.

Requirements:
1. Use Svelte 5 Runes syntax ($state, $derived, $effect) — NOT legacy $: reactive
2. Use TypeScript for all props and logic
3. Define props using `let { prop1, prop2 }: Props = $props()`
4. Style with Tailwind CSS 4 utility classes only
5. Follow the design system colors (primary #1B5E20, secondary #F9A825)
6. Make responsive (mobile-first approach)
7. Add JSDoc comments for the component's purpose and props
8. Place in appropriate directory under `src/lib/components/`
9. Export from the relevant barrel file if one exists

Component to create: $ARGUMENTS
